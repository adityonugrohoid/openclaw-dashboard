[project]
name = "openclaw-dashboard"
version = "2.0.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "psutil>=5.9.0",
]

[project.scripts]
dashboard = "app:main"